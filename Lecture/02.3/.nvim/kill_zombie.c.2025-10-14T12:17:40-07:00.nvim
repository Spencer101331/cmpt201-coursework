#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
#include <sys/wait.h>

int main() {
  pid_t pid = fork();
  if (pid) {
    int wstatus;
    while (waitpid(-1, &wstatus, WNOHANG) > 0) {
      printf("Reaped child with PID %d\n", pid);
    }
  }
}
