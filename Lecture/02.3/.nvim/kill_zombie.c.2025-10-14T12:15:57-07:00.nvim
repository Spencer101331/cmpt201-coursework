#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();
  if (pid) {
    while (waitpid(-1, &wstatus, WNOHANG)) > 0) {
      printf("Reaped child with PID %d\n", pid);
    }
}
