#include <stdio.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();
  for (int i = 0; i < 10; i++){
    if (pid != 0)
      printf("Parent sleeping\n");
    else
      printf("Child sleeping\n");
    sleep(2);
    printf("\n");
  }
  printf("\nDone\n")
  return 0;
}
