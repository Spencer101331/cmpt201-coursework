#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
  pid_t pid = fork();
  if (pid == 0){  // In child
    printf("In child");
    execlp("ls", "ls", "-a", "-l", "-h");
  } else {  // In parent
    printf("In parent");
    execlp("ls", "ls", "-a");
  }

  return 0;
}
