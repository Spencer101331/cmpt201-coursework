#include <stdio.h>
#include <pthread.h>
#include <string.h>
#include <unistd.h>

void *thread_print(void *arg){
  char *str = "Hello this is thread!\n";
  write(1, str,strlen(str));
}
int main() { 
  pthread_t thread;
  pthread_create(&thread, NULL, thread_print, NULL); 
}

